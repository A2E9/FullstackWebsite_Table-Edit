import { WithUnsortDirective } from './with-unsort.directive';

describe('WithUnsortDirective', () => {
  it('should create an instance', () => {
    const directive = new WithUnsortDirective();
    expect(directive).toBeTruthy();
  });
});
